#nullable enable

// Marker interface — concrete impl คือ TutorialMediaData SO subclasses ใน 4_Infrastructure
// View ใช้ pattern matching (`is ISpriteMedia` / `is IVideoMedia`) เพื่อ dispatch
public interface ITutorialMedia { }
